# Safe template only. patrol_node refuses to run this uncalibrated route.
# Generate the deployable route from live AMCL poses with:
#   ros2 launch raspi_car_bringup record_waypoints.launch.py
route:
  name: uncalibrated_template
  frame_id: map
  calibrated: false
  map_sha256: ''
waypoints: []
